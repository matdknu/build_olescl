project:
  type: website
  output-dir: _site

execute:
  echo: false
  warning: false
  message: false

format:
  html:
    theme: default
    css: style.css
    # Nota: Los scripts se agregan manualmente en los archivos HTML
    # No usar include-in-header aquí porque causa errores con archivos .md
